# GPT-6 음성·입술 관측 결합 재테스트 — 2026-09-18

## 결과와 범위

**testvideo1.mp4에서 실제 입술 관측값을 추출하고, 기존 Seungun 음성 분석 결과와 함께 GPT-6 Astra low에 전달하는 테스트에 성공했다.** HTTP 200, `finish_reason=stop`, `validated_grounded_plan`, fallback 없음. GPT 호출과 검증·렌더링은 9.508초였다.

단, **운영에서 승인된 선택 음소용 영상 보조 근거를 만든 테스트는 아니다.** 이번 추가 자료는 영상 전체의 8개 표본에서 얻은 입술 관측값이다. 선택 음소 ‘콩’의 받침 ㅇ에는 음성 정렬 시작·종료 시각이 없어 특정 입술 구간과 연결하지 않았다. A/V sync, 음소별 영상 구간, 정상 발음 기준 대비 편차를 검증하지 않았으므로 입술의 크고 작음·정확성·발음 점수로 변환하지 않았다.

이전 스크립트는 testvideo1에서 음성만 분석하고 `visual_supplement=None`을 전달했다. 따라서 이전의 `NO_PUBLISHABLE_EVIDENCE` 상태만으로 영상 분석이 실행됐다가 실패했다고 해석하면 안 된다.

## 실제 실행

1. RunPod에 보존된 동일 testvideo1의 SHA-256을 이전 음성 분석 입력과 비교해 일치함을 확인했다.
2. 배포된 `FfmpegAvContainerAdapter.probe()`와 `decode_rgb24_sample()`로 컨테이너 검사·RGB 디코딩을 실행했다.
3. 배포된 `MediaPipeLipLandmarkAdapter.detect()`와 고정된 face_landmarker 모델을 사용했다. 운영 composition과 같은 얼굴 검출·존재·추적 threshold 0.5, 동일한 8프레임 전체 영상 샘플링과 자원 한도를 사용했다. 모델과 threshold는 변경하지 않았다.
4. 좌표 자체는 외부로 보내지 않고 프레임 시각, 얼굴 수, 입술 벌림 좌표 비율과 요약 통계만 구성했다.
5. 기존 GPT 비교의 system prompt, 음성 근거, 점수는 그대로 두고 user JSON의 `feedbackEvidence.unboundClipLevelLipObservation`에 관측 자료와 해석 한계를 추가했다. 이 필드는 **격리 실험용 추가 필드**이며 운영 공개 API·영상 보조 계약에 추가한 것이 아니다. 기존 토큰 패킹을 다시 실행하지 않고 이전 음성 입력에 자료를 덧붙였다.
6. `generate_grounded_video_feedback()` → `parse_grounded_advice_plan()` → `validate_grounded_advice_plan()` → `render_grounded_advice_message()`로 응답을 처리했다. `visual_supplement`는 여전히 null이므로 선택 음소의 승인된 영상 주장이나 영상 정상 판정으로 가장하지 않았다.

## 영상 관측

| 항목 | 실제 값 |
| --- | --- |
| 영상 길이 | 4,833ms — 컨테이너 probe 기준 |
| 해상도 | 1080 × 1920 |
| 처리한 프레임 | 8개 |
| 얼굴 1개와 유효 입술 좌표 검출 | 8/8 |
| 복수 얼굴 관측 | 없음 |
| 표본 시각(ms) | 0, 700, 1366, 2066, 2733, 3433, 4100, 4800 |
| 정규화 이미지 좌표의 입술 벌림 비율 최소/최대/평균 | 0.002125 / 0.121075 / 0.040712 |
| 디코딩·입술 추출 등 영상 단계 | 8.917초 |
| 측정 backend revision | mediapipe_1.0.1_face_landmarker_float16_v1 |
| 추론 워커 네트워크 격리 | seccomp_socket_denied |

비율은 기존 코드의 `(아랫입술 normalized y − 윗입술 normalized y) ÷ (오른쪽 입꼬리 normalized x − 왼쪽 입꼬리 normalized x)`다. 이미지 종횡비·머리 자세를 보정한 물리적인 입 벌림 비율이 아니다. 정상 참조값이 없으므로 평균 0.040712를 정상/비정상이나 점수로 해석할 수 없다. 8개 표본은 연속 입술 궤적도 아니다.

이번에는 배포된 기본 입술 좌표 추출 경로를 실행했다. 별도 closed-beta 40점 궤적·교정 추천·영상 품질 및 A/V calibration release까지 모두 통과한 전체 영상 sidecar 검증이 아니다.

## GPT 실제 응답

| 항목 | 값 |
| --- | --- |
| 모델 / reasoning | gpt-6-astra / low |
| HTTP | 200 |
| HTTP 시간 / 검증 포함 시간 | 9.498초 / 9.508초 |
| 입력 / 출력 토큰 | 3,792 / 416 |
| 출력 중 추론 토큰 / 입력 cache hit | 0 / 0 |
| max_completion_tokens / timeout | 512 / 180초 |
| 종료 / 검증 | stop / validated_grounded_plan |
| fallback / 자동 재시도 | 없음 / 0회 |

최종 사용자 문구:

> 이번에 확인할 부분은 ‘콩을’의 ‘콩’ 받침 ㅇ입니다. 제공된 자동 정렬에서는 이 받침에 대응하는 소리와 시점을 찾지 못했습니다. 다만 이것만으로 실제로 받침을 생략했다고 확정할 수는 없습니다. 해당 소리와 연결된 영상 근거도 없어 혀나 입술 움직임의 오류를 판단할 수 없습니다.
>
> 목표 발음에서 ‘콩’의 받침 ㅇ은 소리가 있는 [ŋ]입니다. 자음 소릿값이 없는 ‘을’의 초성 ㅇ과는 다릅니다. 일반적으로 받침 [ŋ]은 혀 뒤쪽을 입천장 뒤쪽에 대어 입안 통로를 막고, 공기를 코로 내보내며 냅니다. 입술을 꼭 닫아서 만드는 소리는 아닙니다.
>
> 먼저 ‘코–콩’을 번갈아 말하며, ‘콩’에서는 모음 뒤에 코로 울리는 받침이 남는지 확인해 보세요. 연습할 때만 받침을 살짝 늘여 ‘콩—’이라고 한 뒤, ‘콩–을 → 콩을’로 자연스럽게 이어 보세요. 받침 뒤에 별도의 ‘그’ 같은 소리를 덧붙이지 않는 것이 목표입니다.
>
> 스스로 녹음해 비교할 때는 ‘코’와 ‘콩’의 끝소리가 구별되는지, ‘콩을’로 이어도 받침의 울림이 유지되는지에 집중해 보세요.

GPT는 입술 수치를 직접 인용하거나 그 수치에 기반한 교정을 제시하지 않았다. 실제 메시지에 관측값이 포함돼 전송됐음은 확인했지만, 문구만으로 각 수치가 모델 판단에 미친 영향을 증명할 수는 없다. 해당 음소와 연결된 영상 근거가 없다는 설명은 이번 실험의 한계와 일치한다.

## 시간 해석

영상 단계 8.917초와 GPT 단계 9.508초의 합은 **18.425초**다. 두 단계를 따로 실행한 시간의 합이며, 중간 조사·스크립트 수정 시간과 기존 Seungun 추론·채점·S3·콜백·프론트 처리는 제외했다. 새 영상의 프론트 E2E 18초를 의미하지 않는다. 이번에는 채점을 다시 호출하지 않고 기존 점수를 유지했다.

이전 음성 전용 GPT 10.665초보다 이번 호출이 짧았지만, 각 1회이고 입력도 달라 영상 자료 추가로 빨라졌다고 해석할 수 없다. 이번 입력으로 CLOVA를 재호출하지 않았으므로 영상 결합 조건에서 CLOVA와의 속도·품질 비교는 하지 않았다.

## 근거와 변경 범위

- 입력 메시지 SHA-256: `704242e41ee5137d4c8a357448d4118120c20b78e50e0661844279d065717f7b`.
- 원격 결과 폴더: `/tmp/feedback-transfer-stage-20260918/gpt6-visual-20260918T115231Z`.
- `lip-observation.json`: 실제 측정값. `gpt-messages.json`: 전송된 system/user 메시지. `gpt-response-512.json`: 실제 API 응답. `report.json`: 단계별 결과. report의 `ready.gptCalled=false`는 영상 추출 직후 체크포인트이며, 이후 `gptAttempts`에 실제 호출 성공이 기록돼 있다.
- 로컬 실험 스크립트: `C:\Users\Public\Documents\ESTsoft\CreatorTemp\clova-gpt6-comparison-20260918\visual_remote.py`, `visual_feedback_remote.py`, `run_comparison.py`.
- 로컬 결과: 같은 폴더의 `visual-feedback-comparison-output.jsonl`.
- 최초 실행의 상수 import 경로와 입력 설명에 포함된 슬래시를 수정했다. 슬래시는 기존 개인정보 검증기에서 파일 경로로 오인됐으므로 설명만 자연어로 바꿨다. 실패한 준비 실행은 GPT를 호출하지 않았고, 검증기 자체를 완화하지 않았다.
- 운영 코드·설정·프로세스·DB·공개 API·모델 가중치·threshold를 변경하지 않았다. 원본 영상과 키 사본은 보존했다. 원본 미디어·전체 얼굴 좌표·인증키는 GPT 메시지에 포함하지 않았다.

현재 샘플의 남은 핵심 제약은 **선택 음소의 시간 정렬 부재**다. 음소별 입술 교정을 검증하려면 실제로 시간 연결이 성립하는 근거와 해당 영상 구간이 필요하다. 이번 결과를 통과시키려고 선택 음소·시각을 임의로 바꾸지는 않았다.
