package org.example.voice.training.domain.type;

public enum RecordingDeletionReason {
    RECORDING_DELETED,
    SESSION_CANCELED,
    HISTORY_DELETED,
    USER_WITHDRAWN,
    UPLOAD_EXPIRED,
    ANALYSIS_COMPLETED
}
