package org.example.voice.analysis.domain.type;

/** Configuration availability, not a live infrastructure health signal. */
public enum AnalysisConfigurationStatus {
    NOT_CONFIGURED,
    CONFIGURED
}
